# This directory holds raw documentation files.
# 
# Supported formats:
#   - Markdown (.md) from react.dev (hooks-era)
#   - Markdown (.md) from reactjs.org v17 (class-era)
#
# To load real docs:
#   git clone --depth 1 --branch v17.0.2 https://github.com/reactjs/reactjs.org.git data/raw_docs/legacy
#   git clone --depth 1 https://github.com/reactjs/react.dev.git data/raw_docs/current
#
# Or use the built-in synthetic corpus (no cloning required):
#   from src.ingestion.loader import load_synthetic_react_docs
